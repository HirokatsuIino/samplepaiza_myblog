<%= form_with(model: publishing_term, local: true) do |form| %>
  <% if publishing_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publishing_term.errors.count, "error") %> prohibited this publishing_term from being saved:</h2>

      <ul>
        <% publishing_term.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_publishing_setting_id %>
    <%= form.number_field :client_publishing_setting_id %>
  </div>

  <div class="field">
    <%= form.label :begin_at %>
    <%= form.datetime_select :begin_at %>
  </div>

  <div class="field">
    <%= form.label :end_at %>
    <%= form.datetime_select :end_at %>
  </div>

  <div class="field">
    <%= form.label :flg %>
    <%= form.check_box :flg %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
