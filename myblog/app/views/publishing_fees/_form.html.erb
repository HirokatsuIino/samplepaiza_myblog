<%= form_with(model: publishing_fee, local: true) do |form| %>
  <% if publishing_fee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publishing_fee.errors.count, "error") %> prohibited this publishing_fee from being saved:</h2>

      <ul>
        <% publishing_fee.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.number_field :client_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
