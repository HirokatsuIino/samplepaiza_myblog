<%= form_with(model: notification2, local: true) do |form| %>
  <% if notification2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification2.errors.count, "error") %> prohibited this notification2 from being saved:</h2>

      <ul>
        <% notification2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="field">
    <%= form.label :begin_at %>
    <%= form.date_select :begin_at %>
  </div>

  <div class="field">
    <%= form.label :end_at %>
    <%= form.datetime_select :end_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
